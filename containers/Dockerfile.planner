# Dockerfile para o agente Planner
ARG BASE_IMAGE=geminiclaw-base
FROM ${BASE_IMAGE}
LABEL stage=runtime component=agent-planner

# Sobrescreve o comando padrão para rodar o planner
CMD ["python", "-m", "agents.planner.agent"]
 