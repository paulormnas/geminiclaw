# Dockerfile para o agente Reviewer
ARG BASE_IMAGE=geminiclaw-base
FROM ${BASE_IMAGE}
LABEL stage=runtime component=agent-reviewer

# Sobrescreve o comando padrão para rodar o reviewer
CMD ["python", "-m", "agents.reviewer.agent"]
