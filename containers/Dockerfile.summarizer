# Dockerfile para o agente summarizer do GeminiClaw
ARG BASE_IMAGE=geminiclaw-base
FROM ${BASE_IMAGE}
LABEL stage=runtime component=agent-summarizer

# Sobrescreve o comando padrão para rodar o summarizer
CMD ["python", "-m", "agents.summarizer.agent"]
