# Dockerfile para o agente Validator
ARG BASE_IMAGE=geminiclaw-base
FROM ${BASE_IMAGE}
LABEL stage=runtime component=agent-validator

# Sobrescreve o comando padrão para rodar o validator
CMD ["python", "-m", "agents.validator.agent"]
